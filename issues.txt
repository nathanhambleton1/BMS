The only thing I want you to change is the blocks on the P45B_CCCV simulink model. I want you to change the  charge logic section. I want you to do this with simple logic gates if possible but instead of just having it go on a charge and discharge cycle I want to add variables to the matlab script that left you control discharge pulses like a certian amount of amps of like how you have it now with C rates and then periods and duty cycle of the system which you can just input and use in a pulse generator block. I want this pulse to take priorety right I want it to always discharge whenever that pulse happens and then just charger whenever it can. Now one thing is that charger block will just output charger forever getting smaller and smaller so if we could have some way to just switch it off and then just let the batteries sit instead of just constnatly charging them like a real battery by just putting something in the logic of a switch or soemthing. keep in mind this should be as simple as possible. Now one thing with the switches because the batties are not ideal they can seem charge but decay or the capactior evens out or something and then cause the threshhold to go down under the values and then turn the switch back on and cause rapid switching with i dont want to happen so to combat that add something like maybe a threshold like it will only turn on again once fully charged when it drops below a certain values. I also want you to see if there is a way for when you make the battery pack to instead of stopping at the module level you keep going to the full pack (you would just use 1 module and module assempbly to make it) but it would that have ports on the pack level that output the SOC and cell voltage that we could easliy tap into so we dont need to use the probe.
